name: Feature request
description: Suggest a focused improvement to archived-session recovery
labels: [enhancement]
body:
  - type: markdown
    attributes:
      value: This plugin intentionally does not replace native search, archive, fork, or deletion features.
  - type: textarea
    id: need
    attributes: { label: User need }
    validations: { required: true }
